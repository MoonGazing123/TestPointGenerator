# TestPointGenerator 使用手册

## 简介

TestPointGenerator 是一个带图形界面的测试点生成工具，专为 OI 出题人设计。它可以根据你定义的模板，自动生成多组 `.in` 输入文件，调用你的标准程序生成 `.out` 输出文件，并一键打包成 `.zip` 压缩包，方便直接上传至 OJ 平台。

---

## 下载与安装

### 方式一：下载 exe 文件（推荐，无需 Python 环境）
1. 访问本仓库的 [Releases](https://github.com/你的用户名/TestPointGenerator/releases) 页面。
2. 下载最新版本的 `TestPointGenerator.exe` 文件。
3. 双击运行即可。

### 方式二：从源码运行（需 Python 环境）
```bash
git clone https://github.com/你的用户名/TestPointGenerator.git
cd TestPointGenerator
pip install -r requirements.txt
python src/main.py
```

# 具体使用流程：
```text
快速开始
点击「Browse」选择你的 std.exe 文件。

std.exe如何生成：

std.exe 就是把你写好的 C++ 标准程序（std.cpp）编译后得到的可执行文件。它能让你的测试点生成器自动跑出正确答案。

-----------------------------------------------------------

在「数据生成模板」框中输入模板，例如 n 1 100 a 1 100。

输入你要生成的数据组数，例如 10。

点击「Generate Test Points」。

生成完成后，当前目录会出现 test_data.zip，里面包含所有 .in 和 .out 文件。

-----------------------------------------------------------------

模板语法详解：

模板的格式为：变量名 最小值 最大值

每个变量由三个部分组成：变量名、最小值、最大值

多个变量之间用空格分隔

程序会为每个变量在 [最小值, 最大值] 区间内生成一个随机整数

变量名的意义
变量名本身不会影响生成的数值，但它有以下几个重要作用：

1. 让模板可读
你可以通过变量名知道生成的数据分别对应题目中的哪个量。例如：

模板 n 1 100 m 1 1000 中，n 和 m 分别代表点数、边数，一眼就懂。

如果写成 a 1 100 b 1 1000，虽然也能工作，但含义不够清晰。

2. 方便后续修改
当你需要调整某个变量的范围时，变量名可以帮你快速定位。例如：

如果要修改点的数量，直接找到 n 这一项。

如果没有名字，你只能靠顺序去猜测。

3. 方便他人理解
如果你把模板发给别人看，或者写进题解里，变量名能让他人迅速理解你的数据生成逻辑。

4. 变量名可以任意起
只要不包含空格，你可以使用任何字母组合。例如：x 1 10、y 1 20、q 1 100。程序只关心后面的最小值、最大值，不关心你叫什么。

示例说明
模板输入	生成的数据示例	说明
n 1 100	42	生成一个 1 到 100 之间的随机数，代表点数
n 1 100 a 1 100	42 87	生成两个 1 到 100 之间的随机数
n 1 10 m 1 20	7 15	生成两个不同范围的随机数
n 5 5	5	固定值 5（最小值和最大值相等）
常见场景模板
题目类型	模板示例	说明
A+B Problem	a 1 100 b 1 100	生成两个 1~100 的随机数
数组求和	n 1 1000 a 1 10000	先随机数组长度，再随机数组元素
图论题	n 1 100 m 1 1000	生成点和边的数量
进阶功能
1. 固定值
如果你想让某个变量始终为固定值，可以将最小值和最大值设为相同：

text
n 10 10 a 1 100
这样 n 始终为 10，a 在 1~100 之间随机。

2. 多组不同范围
你可以为不同变量设置不同范围：

text
n 1 50 m 1 100 k 1 10
3. 纯固定值
如果不需要随机生成，可以直接写固定值（不带空格）：

text
5 10 15
这样每次生成的数据都是 5 10 15。

常见问题
Q: 生成的 .out 文件内容不对怎么办？
A: 检查你的 std.exe 是否正确读取了 .in 文件的格式，以及输出格式是否与题目要求一致。

Q: 可以生成字符串或浮点数吗？
A: 当前版本只支持整数生成。

Q: 生成的压缩包在哪？
A: 在程序所在的目录下，文件名为 test_data.zip。

Q: 支持多组测试点使用不同模板吗？
A: 当前版本所有测试点使用同一模板，后续版本会支持按组定制。

更新日志
日期	版本	更新内容
2026-08-10	v1.0.0	初始版本发布
反馈与建议
如果你在使用过程中遇到问题，或有改进建议，欢迎在本仓库的 Issues 页面提出
```
